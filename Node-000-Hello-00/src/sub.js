// 함수 모듈
const sum = (num1, num2) => num1 + num2;
const sub = (num1, num2) => num1 - num2;
const mul = (num1, num2) => num1 * num2;

const Rt = () => "대한민국";

export default Rt;
export { sum, sub, mul };
